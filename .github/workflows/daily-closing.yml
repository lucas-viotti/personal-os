# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
name: Daily Closing

# AI-powered end-of-day summary with activity analysis
# Requires secrets: ATLASSIAN_*, LLM_*, SLACK_*

on:
  schedule:
    # 5:50 PM BRT (20:50 UTC) Mon-Fri
    - cron: '50 20 * * 1-5'
  workflow_dispatch:

env:
  LLM_MODEL: "gpt-4o-mini"

jobs:
  daily-closing:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ============================================
      # STEP 1: GATHER TODAY'S ACTIVITY
      # ============================================
      
      - name: Fetch Jira Activity
        id: jira
        continue-on-error: true
        env:
          ATLASSIAN_DOMAIN: ${{ secrets.ATLASSIAN_DOMAIN }}
          ATLASSIAN_EMAIL: ${{ secrets.ATLASSIAN_EMAIL }}
          ATLASSIAN_API_TOKEN: ${{ secrets.ATLASSIAN_API_TOKEN }}
          JIRA_PROJECT: ${{ secrets.JIRA_PROJECT }}
        run: |
          if [ -z "$ATLASSIAN_DOMAIN" ] || [ -z "$JIRA_PROJECT" ]; then
            echo "jira_data=_Jira not configured_" >> $GITHUB_OUTPUT
            echo "jira_count=0" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          echo "Fetching today's Jira activity from $ATLASSIAN_DOMAIN..."
          echo "User email: $ATLASSIAN_EMAIL"
          
          # Use email directly in JQL - this is the most reliable approach
          JQL="project = ${JIRA_PROJECT} AND (assignee = \"${ATLASSIAN_EMAIL}\" OR reporter = \"${ATLASSIAN_EMAIL}\" OR watcher = \"${ATLASSIAN_EMAIL}\" OR creator = \"${ATLASSIAN_EMAIL}\") AND updated >= startOfDay() ORDER BY updated DESC"
          
          echo "JQL: $JQL"
          JQL_ENCODED=$(echo "$JQL" | jq -sRr @uri)
          
          echo "Encoded JQL: $JQL_ENCODED"
          
          JIRA_RESPONSE=$(curl -s -w "\n%{http_code}" -u "${ATLASSIAN_EMAIL}:${ATLASSIAN_API_TOKEN}" \
            "https://${ATLASSIAN_DOMAIN}/rest/api/3/search?jql=${JQL_ENCODED}&maxResults=30&fields=key,summary,status,updated")
          
          # Extract HTTP code and response body
          HTTP_CODE=$(echo "$JIRA_RESPONSE" | tail -1)
          RESPONSE_BODY=$(echo "$JIRA_RESPONSE" | sed '$d')
          
          echo "HTTP Code: $HTTP_CODE"
          echo "Response Body: $RESPONSE_BODY"
          
          # Check HTTP status
          if [ "$HTTP_CODE" != "200" ]; then
            echo "HTTP Error: $HTTP_CODE"
            echo "Falling back to project-wide query..."
            JQL="project = ${JIRA_PROJECT} AND updated >= startOfDay() ORDER BY updated DESC"
            JQL_ENCODED=$(echo "$JQL" | jq -sRr @uri)
            JIRA_RESPONSE=$(curl -s -u "${ATLASSIAN_EMAIL}:${ATLASSIAN_API_TOKEN}" \
              "https://${ATLASSIAN_DOMAIN}/rest/api/3/search?jql=${JQL_ENCODED}&maxResults=30&fields=key,summary,status,updated")
            RESPONSE_BODY="$JIRA_RESPONSE"
            echo "Fallback Response: $RESPONSE_BODY"
          fi
          
          # Check for JQL errors
          ERROR_MSG=$(echo "$RESPONSE_BODY" | jq -r '.errorMessages[0] // empty' 2>/dev/null)
          if [ -n "$ERROR_MSG" ]; then
            echo "JQL Error: $ERROR_MSG"
            RESPONSE_BODY='{"issues":[]}'
          fi
          
          JIRA_COUNT=$(echo "$RESPONSE_BODY" | jq -r '.issues | length' 2>/dev/null || echo "0")
          JIRA_ISSUES=$(echo "$RESPONSE_BODY" | jq -r '.issues[]? | "• \(.key): \(.fields.summary) [\(.fields.status.name)]"' 2>/dev/null || echo "")
          
          echo "Found $JIRA_COUNT issues"
          
          if [ -z "$JIRA_ISSUES" ] || [ "$JIRA_COUNT" = "0" ]; then
            JIRA_ISSUES="_No Jira activity today_"
          fi
          
          echo "jira_data<<EOF" >> $GITHUB_OUTPUT
          echo "$JIRA_ISSUES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          echo "jira_count=$JIRA_COUNT" >> $GITHUB_OUTPUT

      - name: Fetch Confluence Activity
        id: confluence
        continue-on-error: true
        env:
          ATLASSIAN_DOMAIN: ${{ secrets.ATLASSIAN_DOMAIN }}
          ATLASSIAN_EMAIL: ${{ secrets.ATLASSIAN_EMAIL }}
          ATLASSIAN_API_TOKEN: ${{ secrets.ATLASSIAN_API_TOKEN }}
          CONFLUENCE_SPACES: ${{ secrets.CONFLUENCE_SPACES }}
        run: |
          if [ -z "$ATLASSIAN_DOMAIN" ] || [ -z "$CONFLUENCE_SPACES" ]; then
            echo "confluence_data=_Confluence not configured_" >> $GITHUB_OUTPUT
            echo "confluence_count=0" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          echo "Fetching today's Confluence activity..."
          
          ALL_PAGES=""
          TOTAL_COUNT=0
          TODAY_START=$(date -u +%Y-%m-%dT00:00:00)
          
          for SPACE in $(echo $CONFLUENCE_SPACES | tr ',' ' '); do
            RESPONSE=$(curl -s -u "${ATLASSIAN_EMAIL}:${ATLASSIAN_API_TOKEN}" \
              "https://${ATLASSIAN_DOMAIN}/wiki/rest/api/content?spaceKey=${SPACE}&expand=history.lastUpdated&limit=20&orderby=history.lastUpdated%20desc" \
              2>/dev/null || echo '{"results":[]}')
            
            PAGES=$(echo "$RESPONSE" | jq -r --arg today "$TODAY_START" \
              '.results[]? | select(.history.lastUpdated.when > $today) | "• \(.title)"' 2>/dev/null || echo "")
            
            if [ -n "$PAGES" ]; then
              ALL_PAGES="${ALL_PAGES}${PAGES}\n"
              COUNT=$(echo "$PAGES" | grep -c "•" || echo "0")
              TOTAL_COUNT=$((TOTAL_COUNT + COUNT))
            fi
          done
          
          if [ -z "$ALL_PAGES" ] || [ "$TOTAL_COUNT" = "0" ]; then
            ALL_PAGES="_No Confluence edits today_"
          fi
          
          echo "confluence_data<<EOF" >> $GITHUB_OUTPUT
          echo -e "$ALL_PAGES" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          echo "confluence_count=$TOTAL_COUNT" >> $GITHUB_OUTPUT

      # Note: Slack message fetching requires user token (restricted in many companies)
      # For full Slack access, use the local script: scripts/logbook-local.py

      - name: Read Current Tasks
        id: tasks
        run: |
          # Get all active tasks with details
          TASK_DETAILS=""
          for file in Tasks/*.md; do
            [ -f "$file" ] || continue
            
            TITLE=$(grep "^title:" "$file" | sed 's/title: //' | head -1)
            PRIORITY=$(grep "^priority:" "$file" | sed 's/priority: //' | head -1)
            STATUS=$(grep "^status:" "$file" | sed 's/status: //' | head -1)
            
            case $STATUS in
              n) STATUS_TEXT="Not Started"; STATUS_EMOJI="🔴" ;;
              s|ip) STATUS_TEXT="In Progress"; STATUS_EMOJI="🟡" ;;
              b) STATUS_TEXT="Blocked"; STATUS_EMOJI="🟠" ;;
              d) STATUS_TEXT="Done"; STATUS_EMOJI="✅" ;;
              *) STATUS_TEXT="Unknown"; STATUS_EMOJI="⚪" ;;
            esac
            
            TASK_DETAILS="${TASK_DETAILS}• ${STATUS_EMOJI} [${PRIORITY}] ${TITLE}\n"
          done
          
          # Count by priority
          P0_COUNT=$(grep -l "priority: P0" Tasks/*.md 2>/dev/null | wc -l | tr -d ' ')
          P1_COUNT=$(grep -l "priority: P1" Tasks/*.md 2>/dev/null | wc -l | tr -d ' ')
          
          # P0 tasks - separated by status (no emoji on each line)
          P0_NOT_STARTED=""
          P0_IN_PROGRESS=""
          for file in $(grep -l "priority: P0" Tasks/*.md 2>/dev/null || echo ""); do
            TITLE=$(grep "^title:" "$file" | sed 's/title: //' | head -1)
            STATUS=$(grep "^status:" "$file" | sed 's/status: //' | head -1)
            case $STATUS in
              n) P0_NOT_STARTED="${P0_NOT_STARTED}• ${TITLE}\n" ;;
              s|ip|b) P0_IN_PROGRESS="${P0_IN_PROGRESS}• ${TITLE}\n" ;;
            esac
          done
          
          # P1 tasks - separated by status (no emoji on each line)
          P1_NOT_STARTED=""
          P1_IN_PROGRESS=""
          for file in $(grep -l "priority: P1" Tasks/*.md 2>/dev/null || echo ""); do
            TITLE=$(grep "^title:" "$file" | sed 's/title: //' | head -1)
            STATUS=$(grep "^status:" "$file" | sed 's/status: //' | head -1)
            case $STATUS in
              n) P1_NOT_STARTED="${P1_NOT_STARTED}• ${TITLE}\n" ;;
              s|ip|b) P1_IN_PROGRESS="${P1_IN_PROGRESS}• ${TITLE}\n" ;;
            esac
          done
          
          echo "task_details<<EOF" >> $GITHUB_OUTPUT
          echo -e "$TASK_DETAILS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          
          echo "p0_count=$P0_COUNT" >> $GITHUB_OUTPUT
          echo "p1_count=$P1_COUNT" >> $GITHUB_OUTPUT
          
          echo "p0_not_started<<EOF" >> $GITHUB_OUTPUT
          echo -e "$P0_NOT_STARTED" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          
          echo "p0_in_progress<<EOF" >> $GITHUB_OUTPUT
          echo -e "$P0_IN_PROGRESS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          
          echo "p1_not_started<<EOF" >> $GITHUB_OUTPUT
          echo -e "$P1_NOT_STARTED" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          
          echo "p1_in_progress<<EOF" >> $GITHUB_OUTPUT
          echo -e "$P1_IN_PROGRESS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      # ============================================
      # STEP 2: AI ANALYSIS
      # ============================================
      
      - name: Generate AI Update Suggestions
        id: ai_analysis
        continue-on-error: true
        env:
          LLM_API_URL: ${{ secrets.LLM_API_URL }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
          LLM_MODEL_SECRET: ${{ secrets.LLM_MODEL }}
          JIRA_COUNT: ${{ steps.jira.outputs.jira_count }}
          JIRA_DATA: ${{ steps.jira.outputs.jira_data }}
          CONFLUENCE_COUNT: ${{ steps.confluence.outputs.confluence_count }}
          CONFLUENCE_DATA: ${{ steps.confluence.outputs.confluence_data }}
          TASK_DETAILS: ${{ steps.tasks.outputs.task_details }}
        run: |
          MODEL="${LLM_MODEL_SECRET:-${{ env.LLM_MODEL }}}"
          
          if [ -z "$LLM_API_KEY" ]; then
            echo "suggestions=_AI analysis not configured. Add LLM_API_KEY secret._" >> $GITHUB_OUTPUT
            exit 0
          fi
          
          # Build prompt using heredoc
          read -r -d '' PROMPT << 'PROMPT_END' || true
          You are a productivity assistant helping a Product Manager log their end-of-day progress.
          
          IMPORTANT RULES:
          - Suggest ONLY 2-3 specific task updates to log (no more!)
          - Match today's Jira/Confluence activity to existing tasks
          - Flag any activity that doesn't connect to a task
          - Use Slack mrkdwn: bold with *, italics with _
          - Use status emoji: 🔴 (not started), 🟡 (in progress), ✅ (done today)
          
          OUTPUT FORMAT (exactly this, no extra headers):
          • [emoji] *Task name*
          _[Specific update to log based on today's activity]_
          
          • [emoji] *Another task*
          _[What progress was made or should be noted]_
          
          DO NOT include:
          - "Suggested Updates:" or similar headers
          - More than 3 items
          - Generic summaries - be specific to the activity shown
          PROMPT_END
          
          PROMPT="$PROMPT

          TODAY: $(date +'%A, %B %d, %Y')
          
          TODAY'S ACTIVITY:
          Jira ($JIRA_COUNT tickets): $JIRA_DATA
          Confluence ($CONFLUENCE_COUNT pages): $CONFLUENCE_DATA
          
          CURRENT TASKS:
          $TASK_DETAILS"

          API_URL="${LLM_API_URL:-https://api.openai.com}"
          
          echo "Calling LLM API at $API_URL..."
          
          AI_RESPONSE=$(curl -s -X POST "${API_URL}/v1/chat/completions" \
            -H "Authorization: Bearer $LLM_API_KEY" \
            -H "Content-Type: application/json" \
            -d "$(jq -n --arg prompt "$PROMPT" --arg model "$MODEL" \
              '{
                "model": $model,
                "messages": [{"role": "user", "content": $prompt}],
                "max_tokens": 600,
                "temperature": 0.7
              }')" 2>&1)
          
          echo "API Response: $AI_RESPONSE"
          
          AI_SUGGESTIONS=$(echo "$AI_RESPONSE" | jq -r '.choices[0].message.content // empty' 2>/dev/null)
          
          if [ -z "$AI_SUGGESTIONS" ]; then
            ERROR_MSG=$(echo "$AI_RESPONSE" | jq -r '.error.message // empty' 2>/dev/null)
            if [ -n "$ERROR_MSG" ]; then
              AI_SUGGESTIONS="_AI error: ${ERROR_MSG}_"
            else
              AI_SUGGESTIONS="_AI analysis unavailable_"
            fi
          fi
          
          echo "suggestions<<EOF" >> $GITHUB_OUTPUT
          echo "$AI_SUGGESTIONS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      # ============================================
      # STEP 3: BUILD AND SEND REPORT
      # ============================================
      
      - name: Build Slack Message
        id: report
        env:
          JIRA_COUNT: ${{ steps.jira.outputs.jira_count }}
          JIRA_DATA: ${{ steps.jira.outputs.jira_data }}
          CONFLUENCE_COUNT: ${{ steps.confluence.outputs.confluence_count }}
          CONFLUENCE_DATA: ${{ steps.confluence.outputs.confluence_data }}
          P0_COUNT: ${{ steps.tasks.outputs.p0_count }}
          P1_COUNT: ${{ steps.tasks.outputs.p1_count }}
          P0_NOT_STARTED: ${{ steps.tasks.outputs.p0_not_started }}
          P0_IN_PROGRESS: ${{ steps.tasks.outputs.p0_in_progress }}
          P1_NOT_STARTED: ${{ steps.tasks.outputs.p1_not_started }}
          P1_IN_PROGRESS: ${{ steps.tasks.outputs.p1_in_progress }}
          AI_SUGGESTIONS: ${{ steps.ai_analysis.outputs.suggestions }}
        run: |
          REPORT="*📊 Daily Closing — $(date +'%A, %B %d, %Y')*\n\n"
          
          # Today's Activity with details
          REPORT+="*📈 Today's Activity*\n"
          if [ "${JIRA_COUNT:-0}" -gt 0 ]; then
            REPORT+="*📋 Jira (${JIRA_COUNT}):*\n${JIRA_DATA}\n\n"
          else
            REPORT+="• 📋 Jira: _No activity today_\n"
          fi
          
          if [ "${CONFLUENCE_COUNT:-0}" -gt 0 ]; then
            REPORT+="*📝 Confluence (${CONFLUENCE_COUNT}):*\n${CONFLUENCE_DATA}\n\n"
          else
            REPORT+="• 📝 Confluence: _No activity today_\n"
          fi
          REPORT+="• 💬 Slack: _Ask Cursor IDE for context_\n\n"
          
          REPORT+="━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n\n"
          
          # P0 Tasks - grouped by status
          REPORT+="*🚨 P0 Tasks (Do Today):* ${P0_COUNT:-0}\n"
          if [ -n "$P0_NOT_STARTED" ] && [ "$P0_NOT_STARTED" != "\n" ]; then
            REPORT+="*🔴 Not started*\n"
            REPORT+="${P0_NOT_STARTED}"
          fi
          if [ -n "$P0_IN_PROGRESS" ] && [ "$P0_IN_PROGRESS" != "\n" ]; then
            REPORT+="*🟡 In Progress*\n"
            REPORT+="${P0_IN_PROGRESS}"
          fi
          if [ "${P0_COUNT:-0}" -eq 0 ]; then
            REPORT+="_None_\n"
          fi
          REPORT+="\n"
          
          # P1 Tasks - grouped by status
          REPORT+="*⚡ P1 Tasks (This Week):* ${P1_COUNT:-0}\n"
          if [ -n "$P1_NOT_STARTED" ] && [ "$P1_NOT_STARTED" != "\n" ]; then
            REPORT+="*🔴 Not started*\n"
            REPORT+="${P1_NOT_STARTED}"
          fi
          if [ -n "$P1_IN_PROGRESS" ] && [ "$P1_IN_PROGRESS" != "\n" ]; then
            REPORT+="*🟡 In Progress*\n"
            REPORT+="${P1_IN_PROGRESS}"
          fi
          if [ "${P1_COUNT:-0}" -eq 0 ]; then
            REPORT+="_None_\n"
          fi
          REPORT+="\n"
          
          REPORT+="━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n\n"
          
          # AI Suggestions
          REPORT+="*💡 Suggested Task Updates*\n"
          REPORT+="${AI_SUGGESTIONS:-_AI analysis not available_}\n\n"
          
          REPORT+="━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\n"
          REPORT+="_Open Cursor IDE to analyze Slack activity or apply task updates_"
          
          echo "report<<EOF" >> $GITHUB_OUTPUT
          echo -e "$REPORT" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Send to Slack
        uses: slackapi/slack-github-action@v1.27.0
        with:
          channel-id: ${{ secrets.SLACK_CHANNEL_ID }}
          payload: |
            {
              "text": "Daily Closing",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": ${{ toJSON(steps.report.outputs.report) }}
                  }
                }
              ]
            }
        env:
          SLACK_BOT_TOKEN: ${{ secrets.SLACK_BOT_TOKEN }}
