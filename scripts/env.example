# =============================================================================
# Logbook Local - Configuration
# =============================================================================
# Copy this file to ../.env (in the repo root) and fill in your values.
# Never commit your actual .env file!

# -----------------------------------------------------------------------------
# Atlassian Configuration (Jira & Confluence)
# -----------------------------------------------------------------------------
ATLASSIAN_DOMAIN=your-company.atlassian.net
ATLASSIAN_EMAIL=your.email@company.com
ATLASSIAN_API_TOKEN=your-atlassian-api-token

# Jira project key to monitor
JIRA_PROJECT=PROJ

# Confluence spaces to check (comma-separated)
CONFLUENCE_SPACES=TEAM,DOCS

# -----------------------------------------------------------------------------
# LLM Configuration (OpenAI-compatible API)
# -----------------------------------------------------------------------------
# Works with OpenAI, Azure OpenAI, Anthropic proxy, local LLMs, NuLiteLLM, etc.
LLM_API_URL=https://api.openai.com
LLM_API_KEY=your-api-key
LLM_MODEL=gpt-4o-mini

# -----------------------------------------------------------------------------
# Slack Configuration
# -----------------------------------------------------------------------------
# Bot token for posting messages (required)
SLACK_BOT_TOKEN=xoxb-your-bot-token

# Channel or User ID to post to (your user ID for DMs: U...)
SLACK_CHANNEL_ID=U1234567890

# Optional: User token for full Slack access (search:read scope)
# If not set, Slack activity will be limited or unavailable
SLACK_USER_TOKEN=xoxp-your-user-token

# Optional: Path to Slack MCP OAuth token storage
# For Nubankers using the internal slack-mcp-server
# The script will try to read the OAuth token from this location
SLACK_MCP_TOKEN_PATH=~/.slack-mcp/token.json

# -----------------------------------------------------------------------------
# Paths (usually auto-detected)
# -----------------------------------------------------------------------------
# TASKS_DIR=/path/to/your/Tasks

